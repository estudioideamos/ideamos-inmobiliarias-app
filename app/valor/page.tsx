import type { Metadata } from "next";
import InternalPage from "../components/InternalPage";
export const metadata: Metadata = { title: "Valor para tu inmobiliaria | Ideamos", description: "Una mejor presencia digital, más autonomía operativa y oportunidades con mayor contexto." };
export default function Page() { return <InternalPage pageKey="valor" />; }